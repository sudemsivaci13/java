# 02 - ATM System

A CLI-based ATM simulation managing bank balance transactions (deposit, withdrawal, and balance inquiry) through control flow structures (`while`, `if-else`) and edge-case balance validation.
